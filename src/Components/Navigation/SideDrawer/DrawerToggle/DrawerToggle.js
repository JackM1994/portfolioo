import React from 'react';
import './DrawerToggle.css';
import { checkPropTypes } from 'prop-types';

const drawerToggle = (props) => {
    <div className="drawerToggle" onClick={checkPropTypes.clicked}>
        <div></div>
        <div></div>
        <div></div>
    </div>
}

export default drawerToggle;